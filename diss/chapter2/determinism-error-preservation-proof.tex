\TODO{Figure out if we even actually need this property, and if so,
  what to do here.  If we do need it, then do we only need to handle
  E-Eval-Ctxt and E-Put-Err, or do we need to look at all the cases?}
